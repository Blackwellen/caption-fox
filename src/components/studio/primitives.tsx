// Shared Studio layout tokens and small primitives. Every Studio surface aligns
// to these so the header, KPI strip, filter bar and content grid share one
// width and one gutter — the same contract Campaigns uses.

export { CARD, CARD_SHADOW, Avatar, OwnerChip, Panel, formatShortDate, formatNumber } from '@/components/campaigns/primitives'

export const STUDIO_PAGE = 'px-6 py-5 lg:px-8'
